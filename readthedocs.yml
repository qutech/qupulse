version: 2

python:
   version: 3.7
   install:
      - requirements: doc/requirements.txt
      - method: setuptools

sphinx:
   builder: html
   configuration: doc/source/conf.py
   fail_on_warning: true
